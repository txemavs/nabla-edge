# Nabla Edge

Public repository for **NablaEdge** (edge stack), **NablaNet** Home Assistant integration, and shared protocols (e.g. `nabla.menu`).

No secrets, credentials, private IPs, or production entity IDs.

See upcoming `AGENTS.md` for contribution / sanitization rules.
